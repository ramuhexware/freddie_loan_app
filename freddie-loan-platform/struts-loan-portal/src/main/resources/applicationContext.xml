<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context
         http://www.springframework.org/schema/context/spring-context.xsd
         http://www.springframework.org/schema/tx
         http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- ============================================================ -->
    <!-- Component scan — picks up @Service, @Repository etc.        -->
    <!-- ============================================================ -->
    <context:component-scan base-package="com.freddieapp.strutsportal"/>
    <context:annotation-config/>

    <!-- ============================================================ -->
    <!-- Load db2.properties                                          -->
    <!-- ============================================================ -->
    <context:property-placeholder location="classpath:db2.properties"/>

    <!-- ============================================================ -->
    <!-- HikariCP DataSource → IBM DB2                               -->
    <!-- ============================================================ -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <property name="driverClassName"  value="${db2.driverClassName}"/>
        <property name="jdbcUrl"          value="${db2.url}"/>
        <property name="username"         value="${db2.username}"/>
        <property name="password"         value="${db2.password}"/>
        <property name="maximumPoolSize"  value="${db2.pool.maximumPoolSize}"/>
        <property name="minimumIdle"      value="${db2.pool.minimumIdle}"/>
        <property name="connectionTimeout" value="${db2.pool.connectionTimeout}"/>
        <property name="idleTimeout"      value="${db2.pool.idleTimeout}"/>
        <property name="maxLifetime"      value="${db2.pool.maxLifetime}"/>
        <property name="poolName"         value="${db2.pool.poolName}"/>
        <property name="connectionTestQuery" value="${db2.pool.connectionTestQuery}"/>
    </bean>

    <!-- ============================================================ -->
    <!-- JdbcTemplate — injected into all DAO beans                  -->
    <!-- ============================================================ -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!-- ============================================================ -->
    <!-- Transaction Manager                                          -->
    <!-- ============================================================ -->
    <bean id="transactionManager"
          class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- ============================================================ -->
    <!-- Microservice base URLs (override with environment variables) -->
    <!-- ============================================================ -->
    <bean id="serviceConfig" class="com.freddieapp.strutsportal.config.ServiceConfig">
        <property name="loanServiceBaseUrl"          value="http://localhost:8081/api/v1"/>
        <property name="customerServiceBaseUrl"      value="http://localhost:8082/api/v1"/>
        <property name="underwritingServiceBaseUrl"  value="http://localhost:8083/api/v1"/>
        <property name="reportServiceBaseUrl"        value="http://localhost:8087/api/v1"/>
    </bean>

</beans>
